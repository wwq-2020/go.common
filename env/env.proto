syntax = "proto3";

package env;

option go_package = "github.com/wwq-2020/go.common/env";

// Env Env
enum Env {
  // UNDEFINED 未定义
  UNDEFINED = 0;
  // DEV 开发环境
  DEV = 1;
  // QA 测试环境
  QA = 2;
  // PL 预生产环境
  PL = 3;
  // ONLINE 线上环境
  ONLINE = 4;
  // ENV_FAT 功能验收测试环境
  FAT = 5;
  // PERF_TEST 压测环境
  PERF_TEST = 6;
}